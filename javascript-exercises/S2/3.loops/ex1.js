const placesToTravel = ['Japon', 'Venecia', 'Murcia', 'Santander', 'Filipinas', 'Madagascar']

for (let index = 0; index < placesToTravel.length; index++) {
    let place = placesToTravel[index];
    console.log(place);
}